/**
 * Particle System Utilities
 * Screen effects (particles are now handled by ParticleEngine in engine/Particle.js)
 */

/**
 * Shake the screen
 * @param {number} amount - Shake intensity
 */
function shakeScreen(amount = 3) {
    shakeAmount = amount;
}
